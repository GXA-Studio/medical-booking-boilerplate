// STEP 4 — Admin dashboard (implemented in the next phase)
// Protected by middleware.ts — requires authenticated Supabase session

export default function AdminDashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Admin dashboard — coming in Step 4</p>
    </main>
  )
}
