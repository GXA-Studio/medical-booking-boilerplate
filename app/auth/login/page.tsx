// STEP 4 — Auth/login page (implemented with admin dashboard)
// Supabase Auth email+password for clinic admins

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Admin login — coming in Step 4</p>
    </main>
  )
}
